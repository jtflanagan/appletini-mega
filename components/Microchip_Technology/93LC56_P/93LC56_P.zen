"""
93LC56_P

Auto-generated using `pcb search`.
"""

CLK = io(Net)
CS = io(Net)
DI = io(Net)
DO = io(Net)
NU = io(Net)
ORG = io(Net)
VCC = io(Net)
VSS = io(Net)

Component(
    name="93LC56_P",
    symbol=Symbol(library="93LC56_P.kicad_sym"),
    pins={"CLK": CLK, "CS": CS, "DI": DI, "DO": DO, "NU": NU, "ORG": ORG, "VCC": VCC, "VSS": VSS},
)
